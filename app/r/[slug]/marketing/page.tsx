import {notFound} from "next/navigation"; import {getDb} from "@/lib/db"; import TenantBootstrap from "@/components/TenantBootstrap"; import MarketingCentre from "@/components/MarketingCentre"; export const dynamic="force-dynamic"; export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const r=await getDb().restaurant.findFirst({where:{slug,isActive:true},select:{id:true}});if(!r)notFound();return <TenantBootstrap slug={slug}><MarketingCentre/></TenantBootstrap>}