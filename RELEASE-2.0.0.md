# Bite2Eat 2.0.0-alpha.1 — Loyalty & Marketing Centre

Adds configurable loyalty points, automatic customer creation and points awards, customer CRM, coupon creation, campaign builder, and a new `/r/[slug]/marketing` Growth Centre. Run `npx prisma db push` after copying `.env`.
